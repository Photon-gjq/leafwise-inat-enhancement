{
  "name": "leafwise-inat-enhancement",
  "version": "0.12.3",
  "private": true,
  "description": "iNaturalist enhancements for Chrome, Edge and Firefox, built from shared source",
  "license": "UNLICENSED",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "test": "node scripts/test.mjs",
    "check": "npm run build && npm test",
    "amo:status": "node scripts/amo-version-status.mjs",
    "package": "npm run build && node scripts/package.mjs",
    "release:notes": "node scripts/release-notes.mjs",
    "test:layout": "playwright test"
  },
  "devDependencies": {
    "fflate": "0.8.3",
    "@playwright/test": "1.63.0"
  }
}
