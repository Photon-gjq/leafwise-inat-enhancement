# 更新紀錄

## 尚未發佈

## 0.12.5 — 2026-09-22

- 修正上傳頁 AI 面板收合時被插入「全選」表單、撐高整條固定工具列並遮住首排觀察卡片刪除按鈕；收合面板現在直接掛載於工具列並脫離排版流，仍緊跟「全選」且會在視窗縮放時重新定位。
- 新增 Chrome、Edge 與 Firefox 回歸測試，驗證工具列高度不變、首排叉號實際可命中並能刪除卡片。

## 0.12.4 — 2026-09-21

- 修正 iNaturalist 上傳頁改用 API v2 `score_image` multipart 請求後部分候選沒有綜合分數：只在網站原有的 `fields` 投影中補入 `combined_score`，不增加辨識請求，也不改照片、地點、日期或其他欄位。
- 綜合分數改為按上傳卡片／觀察頁精確綁定，不再用短期候選重疊推測來源，避免多張照片並行處理時互相串分；遲到的舊請求也不能覆蓋較新的結果。
- 已取得的分數在目前頁面生命週期內保留，重新打開網站快取的建議選單不再因兩分鐘到期而消失；批次分數模式會在選單先出現時額外等候分數回應，最多四秒。
- 新增 Chrome 與 Firefox 的 API v2 multipart、卡片隔離、快取選單重開及分數持久性瀏覽器回歸測試。

## 0.12.3 — 2026-09-18

- 修正具體觀察頁的 API v2 辨識請求以 observation UUID 傳送、且欄位投影省略 `combined_score` 時分數不顯示：白名單現在接受嚴格 UUID，並只在網站既有請求的回傳欄位投影中補入 `combined_score`；不新增辨識請求，也不改照片、觀察、位置、日期或登入資訊。
- 上傳頁與觀察詳情頁的彩色分數在多行候選列中改為按整列上下居中。

## 0.12.2 — 2026-09-18

- 具體觀察詳情頁的原生 AI 鑑定選單現在可用官方 `.ac.vision` DOM 標記辨認視覺候選；即使瀏覽器隔離環境無法讀取網站的 jQuery 資料快取，也能按 taxon ID 顯示既有 `combined_score`，且不會替手動搜尋列補分。
- 上傳頁與觀察詳情頁的分數外觀改為單純的彩色一位小數；移除膠囊背景、邊框及候選列左側色條，保留非百分比及「不是正確率」提示。

## 0.12.1 — 2026-09-18

- 修正 iNaturalist 把 `inaturalistjs` 改為 ES module 區域變數後綜合分數消失的問題：MAIN world 現在被動旁讀網站原有的 fetch／XHR 辨識回應，不依賴 `window.inaturalistjs`，也不增加或改寫請求與回應。
- 把官方回應中 0–1 的 `combined_score` 正規化為介面使用的 0–100，保留既有 >1–100 相容值；仍只接受 `combined_score` 並按 taxon ID 配對，不使用 `vision_score`。
- 上傳選單已先出現而分數稍後抵達時立即重掃並裝飾；新增 URL 白名單、fetch、XHR、回應不變、重複安裝、遲到事件及清理的三瀏覽器回歸測試。

## 0.12.0 — 2026-09-17

- 上傳頁及觀察詳情頁改為顯示 iNaturalist 原生辨識回應中的 `combined_score`，按分類 ID 配對，不再把純視覺 `vision_score` 當作門檻分數；沿用網站已帶入的照片、地點、日期與登入驗證，不重複呼叫辨識 API。
- 分數介面改為緊湊的一位小數色塊及 3px 行內提示，不顯示容易誤解為機率的百分號；提示文字明確標示為視覺＋時空綜合分數及非正確率。
- 上傳 AI 面板預設收合到原生「全選」右側，快捷按鈕不展開面板即可執行；展開後回到照片區，狀態保存於本機，保留預覽、停止、自動模式及明細。
- 觀察詳情頁在鑑定造成分類變更而網址不變時，清除舊個人次數並強制重新查詢；舊請求回傳不會污染新分類。
- 新增綜合分數擷取／配對、跨頁分數樣式、強制計數刷新、收合面板與緊湊分數版面的回歸測試。

## 0.11.3 — 2026-09-17

- 修正分數模式的回退順序：首選分數未超過門檻時，若網站提供可選的官方「非常確定」上階類群，改為填入該類群；兩者都不符合才保留原值。
- 更新上傳面板與說明文字，明確表達「首選達標，否則回退官方確定類群」的規則，並新增低分、等於門檻及無官方提示的回歸測試。
- 新增 Firefox AMO 自動提交流程：首次可手動提交目前版本，之後由版本標籤在三瀏覽器測試及 GitHub Release 成功後提交同版本至既有公開頁面。
- 提交前檢查 AMO 是否已有相同版本，讓工作流可安全重跑；補上正式安裝與自動更新說明。

## 0.11.2 — 2026-09-13

- 修正「只看官方非常確定提示」仍選取下方第一個最佳建議的問題；現在直接填入官方確定的上階類群。例如官方只確定到葉甲科時，不會再自動填入低分的黃帶芫菁屬。
- 分數模式在首選分數可讀時仍嚴格依門檻選取第一項；分數不可讀但有官方提示時，改為填入官方確定的上階類群。若提示存在但無法確認該類群項目，保留原值。
- 更新面板用語與處理明細，使「最佳建議」和「官方確定類群」不再混淆；補上 Wang.QG 對原始 QG-inat-enhancement 的開發署名。

## 0.11.1 — 2026-09-10

- 新增 Microsoft Edge 專用 ZIP 與安裝／更新說明；共用 Chrome 的 Manifest V3 設定與程式，保留 0.11.0 的完整功能及權限。
- Chrome、Edge、Firefox 改為三個同步建置、測試及打包目標；單一版本標籤同時發布全部套件。
- 新增 Edge 與 Chrome 產物逐檔一致性檢查；Windows CI 用 Microsoft Edge 執行對比、收藏、個人紀錄及上傳頁回歸案例。

## 0.11.0 — 2026-09-10

- 新增生涯未見、已見但該年未見、已見但此地未見、該年首次記錄四種對比模式；年份依觀察日期計算，並維持整個分類分支的判斷。
- 新增歷年月份目標清單、當地日期與項目條件；個人基準與當地候選篩選分開，快取依完整參數共用。
- 新增完整搜尋網址及面板條件收藏、自訂地點組合保存／更新／刪除。
- 內建「中國大陸+港澳臺」、華南（含港澳）、華東、華中、華北、西南、東北、西北等分組；華東不含臺灣，所有成員及官方 ID 可查看。
- 觀察與類群頁的個人次數可點開首次／最近紀錄小卡；按需請求兩筆公開、有日期的觀察，保留查看全部紀錄入口。
- 對比結果可複製或匯出 CSV，包含所有符合名稱篩選的頁面、目前排序及已核驗 Leaf taxa；附完整對比範圍，匯出不增加 API 請求。
- Chrome／Firefox 同步更新，新增跨頁收藏、模式切換、匯出、錯誤重試及紀錄小卡回歸測試；沿用 0.10.2 的上傳版面及停止後設定修正。

## 0.10.2 — 2026-09-10

- 修正上傳 AI 面板把固定的左側批次編輯欄推低，導致日期選單／時間控制超出視窗的問題。面板改放右側照片欄，等待該欄出現再掛載。
- 在 AI 面板點擊控制項時保留網站目前全選狀態，避免 Shadow DOM 事件被網站誤認為點擊空白處。
- 新增 Chrome／Firefox 版面回歸測試：410 張觀察、三種視窗尺寸、日期／時間／地點操作、長明細、捲動及網站重新建立照片欄；GitHub CI 同步執行。

## 0.10.1 — 2026-09-10

- 將 Chrome 與 Firefox 整理成同一個專案、同一份功能原始碼與版本號。
- 集中管理 manifest、背景載入方式及網站 jQuery 存取差異，保留 Firefox 擴充 ID。
- 修正共用 AI 核心在 Firefox 隔離環境中的初始化判斷，並驗證兩個瀏覽器的實際載入。
- 加入兩個瀏覽器共用的回歸測試、建置一致性驗證與可重現的 ZIP／XPI 打包。
- 加入 GitHub CI、標籤發佈、Issue／PR 範本、安裝與維護文件。
- 移除範例欄位中的個人帳號預設；範例按鈕沿用目前填入的使用者。
- 保留 0.10.0 的批次 AI 建議、手動值保留、停止與自動模式，以及既有高階分類對比功能。

## 0.10.0 — 歷史版本摘要

- 新增上傳頁批次 AI 首選、視覺分數顯示、官方提示備援、預覽及自動處理空白觀察。
- 分別產出 Firefox 及 Chrome 版本；此版本的舊成品保留在原本本機輸出目錄。

## 0.9.8 — 歷史版本摘要

- 常用使用者／類群、中文名、個人計數、多地點與全部生物的高階分類對比。

上述歷史摘要不表示本倉庫包含相應 Git 提交或發佈標籤。
